<div class="flash-message transition">
  <% flash.each do |name, msg| %>
    <% if name == "notice" %>
      <div class="ui positive message">
    <% else %>
      <div class="ui negative message">
    <% end %>
      <%= msg %>
      <i class="close icon"></i>
    </div>
  <% end %>
</div>
